# С чего начать

Пять минут от «у меня есть API-ключ» до «я нахожу то, что только что заингестил».

## Что нужно сначала — API-ключ

Brain не принимает анонимный трафик. Получить ключ можно двумя путями:

- **Self-host (dev)** — пропиши `BRAIN_API_KEYS` в `.env` по рецепту локальной разработки из [`inite-brain-service/README.md`](https://github.com/inite-ai/inite-brain-service).
- **Хостинг** — сгенерируй на [Brain app → API keys](/ru/app/keys). Получишь строку вида `brain_<base64>` и `companyId`. Держи их в паре — путь MCP свой на каждого тенанта.

Примеры ниже считают, что заданы:

```bash
export BRAIN_KEY="brain_..."
export BRAIN_URL="https://brain.inite.ai"
```

Примеры используют личную память `user_42`. Передавайте свой постоянный ID пользователя при записи и чтении. Для общей памяти организации не передавайте `userId` в обоих запросах.

## 1. Что-нибудь запомнить

На вход — текст. Brain сохраняет реплику, извлекает сущности и утверждения
и прогоняет каждое через conflict-resolver:

```bash
curl -X POST "$BRAIN_URL/v1/ingest/mention" \
  -H "Authorization: Bearer $BRAIN_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Мария переехала в Берлин в июне и предпочитает утренние слоты.",
    "userId": "user_42"
  }'
```

В ответе — что записано: `extractedEntityIds`, `extractedFactIds`. `skipped: true` значит, что экстрактор не нашёл ничего, что стоило бы записать — нормальный исход для приветствия, а не ошибка.

## 2. Спросить про это

```bash
curl -X POST "$BRAIN_URL/v1/search" \
  -H "Authorization: Bearer $BRAIN_KEY" \
  -H "Content-Type: application/json" \
  -d '{ "query": "где живёт Мария", "userId": "user_42", "limit": 5 }'
```

Вернёт ранжированные попадания по сущностям с подходящими фактами и разбивкой score по этапам.

### Когда утверждение уже известно

Извлечение нужно для прозы. Если запись идёт из поля CRM или потока
событий — заявляй напрямую и не трать LLM: в ответе тогда придёт то, что
**решил** resolver (`INSERTED`, `SUPERSEDED`, `COMPETING`…):

```bash
curl -X POST "$BRAIN_URL/v1/ingest/fact" \
  -H "Authorization: Bearer $BRAIN_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "entityRef": { "vertical": "rent", "id": "cust_42" },
    "predicate": "complained_about",
    "object": "late maintenance response",
    "userId": "user_42",
    "validFrom": "2026-05-05T10:00:00Z",
    "source": { "vertical": "rent", "messageId": "msg_1" }
  }'
```

## 3. Поднять MCP-клиент

Когда REST заработал, наведи MCP-клиент на тот же ключ. Конфиги под каждый клиент — в [настройке MCP](/ru/docs/mcp/setup).

Конфигурация для Claude Desktop (нужны Node.js и npx):

```json
{
  "mcpServers": {
    "brain": {
      "command": "npx",
      "args": [
        "-y",
        "@inite/brain-mcp"
      ],
      "env": {
        "BRAIN_API_KEY": "brain_YOUR_API_KEY",
        "BRAIN_COMPANY_ID": "YOUR_COMPANY_ID"
      }
    }
  }
}
```

## Что читать дальше

- [Битемпоральная модель](/ru/docs/concepts/bitemporal) — почему у каждого факта две оси времени
- [Словарь предикатов](/ru/docs/concepts/predicates) — строительные блоки фактов
- [Разрешение конфликтов](/ru/docs/concepts/conflict-resolution) — что происходит, когда два ингеста спорят
- [POST /v1/search](/ru/docs/api/search) — полная форма запроса и ответа
- [Справочник MCP-инструментов](/ru/docs/mcp/tools) — доступные инструменты, права и схемы
